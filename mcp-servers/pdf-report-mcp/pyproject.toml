[project]
name = "pdf-report-mcp-server"
version = "0.1.0"
description = "Python MCP server for generating PDF reports"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp==1.9.4",
    "fastapi",
    "uvicorn",
    "loguru",
    "playwright",
]
